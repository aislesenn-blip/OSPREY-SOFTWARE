/** @type {import('next').NextConfig} */
const nextConfig = {
  // Do NOT add basePath
  // Do NOT add output: 'export'
  // Do NOT add distDir
  reactStrictMode: true,
};

module.exports = nextConfig;
